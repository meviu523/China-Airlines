# 启动、默认地图、航行动画与本地化（2026-09-14）

## 入口与导航

首次打开当前浏览器标签页时显示独立游戏开始界面，使用原创本地飞机航行素材、分层天空和云层作为场景。存档在后台完成严格读取；读取完成前“进入游戏”禁用，存档需要恢复时只能先进入存档设置。进入游戏后默认打开只读地图浏览页。

普通底部入口固定为：地图、机场装载、机场目录、机队管理、飞机商店、公司组织、经营中心。公司组织是独立页面，不再位于经营中心页签。地图是最左侧且在浏览页保持选中；浏览地图保留底部导航，可直接切换经营页面。“制定路线”只在机场页作为独立高优先级入口，路线规划页继续隐藏全局导航，防止把浏览和派航混为同一状态。标签页内刷新保留已经进入游戏及当前地图/机场页，关闭后重新打开仍显示开始界面；这些会话偏好不进入游戏存档。

此条替换 `CAREER-NAVIGATION.md` 和 `TASKS-FLEET-LOADING.md` 中“机场装载位于最左”和“独立地图隐藏底部导航”的旧约定，不修改核心经营、路线规划或结算规则。

## 航行动画

开始界面和实际在途机场场景共用纯装饰 `FlightSky`。近云、远云使用不同速度横向移动，飞机使用小幅升沉和俯仰；实际在途场景使用已有 `aircraft-flight-v1.png`。`prefers-reduced-motion: reduce` 时停止飞机和云层动画。

动画只读取 `plane.flight` 决定是否显示，不推进模拟时间、不触发抵达、不扣能量、不结算收入。到达与离线收益仍只由 `src/core` 的时间事件结算，地图和动画回调均不得写入经营状态。

## 本地化

首批支持 `zh-CN` 与 `en-US`，默认中文。开始界面、地图、主导航、机场装载、机场/航班目录、路线规划、任务与存档设置使用同一 React 本地化上下文；机场、机型、区域采用稳定 ID 到本地名称的目录。语言可在开始界面和设置内切换（地图页通过顶部存档设置进入），立即更新 `html.lang`、页面方向、页面标题、描述、数字/时间格式，偏好独立保存于 `china-airlines:locale:v1`，不写入或改写经营存档。

核心继续保存稳定 ID 和既有中文经营日志/错误，不依赖 React 或本地化模块。UI 对旧日志和错误做只读翻译；当前存档仍为 v9、IndexedDB 仍为 1，语言切换和新增语言不得放宽 v7/v8 冻结校验或 v9 现役机型校验。不得在核心写入展示语言，也不得用翻译后的机场名作为搜索身份、命令或存档键。机场缩写仍可作为搜索的隐藏匹配字段，但不出现在标题、目录卡片、目的地站牌、详情列表或城市选择项中。

本地化运行时采用注册表架构，详细契约见 [I18N.md](I18N.md)。`zh-CN` 消息表定义语义 `MessageKey` schema，所有正式 locale bundle 必须覆盖相同消息键及相同插值占位符；语言 metadata、机场/机型/洲名及搜索别名随 bundle 注册。语言选择器、偏好校验、页面 metadata、格式化与实体查找不得针对具体语言写条件分支。新增语言以“新增 locale bundle + 注册”为主，不修改 `src/core`。

尚未迁移的旧页面允许经 `compat/legacy-ui.ts` 使用中文源文本兼容翻译，历史核心日志经 `compat/core-text.ts` 只读翻译；两者都属于过渡兼容层。新 UI 必须使用语义 key，不得继续扩大中文源文本 key 表。未知历史文本或缺失实体翻译原样/核心名称回退，不得因此修改或拒绝有效经营状态。

## 验收

浏览器流程覆盖：新标签开始界面及键盘焦点、飞机航行视觉、进入后默认地图、地图按钮最左且选中、英文切换和英文机场搜索、刷新会话恢复、真实在途场景动画，以及减少动态效果。既有经营流程从机场工作区执行，继续验证桌面、844×390、667×375、竖屏提示、导入/导出、刷新恢复和离线启动。

本地化单元测试额外覆盖：注册表 locale 的完整语义键、跨语言插值占位符一致性、未知 locale 回退、实体名称 fallback 和注册语言搜索别名。新增正式语言必须通过同一组检查。
