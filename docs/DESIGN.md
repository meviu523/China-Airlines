# 原剖面直排与主操作右对齐（2026-09-16）

当前机场机舱与主操作布局以 [UNIFIED-AIRCRAFT-CANVAS.md](UNIFIED-AIRCRAFT-CANVAS.md) 为准。本增量按用户确认替换下方历史约定中的固定56%/44%分舱、重复壁面/地板、外壳独立拉伸及家具随乘客改变尺寸：只保留原剖面，按各机型原图地板坐标摆放座椅、托盘与真实订单；全部图层共享1536×590画布、原点及等比例缩放。制定航线与检票起飞右对齐并共用屏幕边缘/底部基线，其他导航及路线工具继续左对齐。

原始素材使用统一配置在构建时注册，外观图由同一对图层合成；未来重画从同一母版导出，不另画完整外观。不改变现役13机型、经营数值、真实容量、订单、存档9、数据库1或结算。验证结果以对应PR及Pages工作流回执为准。

# 独立机型双层美术与旧机型退役（2026-09-16）

本增量按用户确认替换下方历史约定中的“三档机体共用”“客货舱左右并排”“历史机型继续使用和迁移”。当前仅保留初航号及雨燕、苍鹭、信天翁、极光各客运/货运/客货型，共13种，每种都有独立生成的剖面与近侧外观资源。

飞机由两层组成：第一层为完整剖面机身、远侧结构及一直挂载的动态机舱；第二层为近侧蒙皮、机翼和发动机。查看外观/查看机舱仅切换第二层可见性，并同步屏蔽被遮挡机舱的键盘与点击操作，不重新创建订单、座位、人物或分页。商店、机库、装配和地图位图回退使用同一对图层合成的该机型独立外观。地图正常3D模型沿用已确认的A款，这批资源用于2D场景与位图回退。

客货型与初航号的客舱在上、货舱在下，分配舱高56%/44%；纯客、纯货机使用一个完整舱室。各机型独立标定剖面区域与近侧外壳位置。壁面取自各自剖面资源；地板下方45%保持与坐垫平面平行的投影，保留无扶手座椅、椅脚与人物臀部/脚底、货物/托盘接触锚点。实际容量、真实订单、卸载和有限可见锚点分页保持；文字预留独立底部区域。

旧机型及兼容支持移除：当前机型目录不含历史机型，移除其旧能耗、自动值勤豁免、改装和残值分支；存档v1–v6不再导入，v7–v9只有现役机型可恢复。导入前检查机队、仓储、博物馆中的机型，发现退役或未知机型即拒绝，绝不自动换机、删机、缩容或覆盖主存档和备份。v7/v8现役存档仍先经各自冻结校验器再迁移，冻结代码仅用于这些版本的严格校验，不作为旧机型运行或导入入口。已有磁盘记录无法恢复时继续展示恢复入口，不自动重开。

游戏存档仍为9，IndexedDB结构仍为1；无新增依赖，经营数值及离线结算不变。完整资源与生成来源见 `art/aircraft-layers-v4/prompts.md`，坐标见 `src/ui/aircraft-layer-layouts.json`。本地验收记录见 [AIRCRAFT-CABIN.md](AIRCRAFT-CABIN.md)；PR与Pages交付结果以对应工作流回执为准。

# 地板与坐垫平行透视（2026-09-15）

保持侧面剖视及原舱壁、舷窗，只修改地板俯视投影。以现有v3坐垫的宽边和纵深边方向为基准（画布向量385/45与148/-79），地板两组接缝分别朝远处消失点收敛，客货舱使用同一投影参数。地板仍占舱高下方35%，人物、座椅及货物锚点保持。

替换此前地板图片纵向放大588%/476%的实现：仅从原图取无轨道材质，地板接缝和货舱系留轨道用本地SVG投影到逻辑像素，避免客货舱宽度变化拉歪透视；不旋转整间机舱，不采用本轮生成但未确认的背景图。此为美术投影调整，经营、存档、容量、装卸和依赖不变。

# 座椅透视与二维接触点（2026-09-15）

按参考图及补充要求，座椅改为接近正面并进一步朝左约25度的原创无扶手座椅，补齐可见金属前后椅脚；货架/托盘采用浅俯视、可见窄右侧面的透视。运行资源为 `cabin-seat-v3.png`、`cabin-pallet-v3.png`，来源和提示词见 `art/cabin-layered-v3/prompts.md`。

本增量替换此前“人物与座椅同高”和独立前扶手图层的约定。6位乘客分别记录归一化臀部x/y与脚底y：人物臀部同时对齐坐垫承重点的两轴，脚底与椅脚落在同一地板基准；缩放前共同约束人物和座椅边界。货物底部对齐托盘顶面，文字仍在独立下方区域。只改美术投影，不修改经营、容量、订单、存档或结算。

# 分层位图机舱与客货锚点（2026-09-15）

机场装载场景替换暂用通用 SVG 机体与 flex 客货排布，采用三档原创左向加厚机身、独立客舱/货舱内部图、座椅/托盘和真实订单叠层。现代12机型、初始机及历史机型按只读美术配置选择轻型、中型、大型外观；大型两家族共享机体，不宣称真实内部复刻。所有层按最终左向画布坐标同步适配；当前机体 PNG 已朝左，不再镜像。地图3D飞机继续沿航迹定向。

舱内位置由独立锚点配置决定，绘制点不等于经营容量。保留真实容量、订单身份、旧聚合数量、装卸限制和每舱分页；无载荷不绘制乘客货物。标题及翻页位于剖面下沿，避免与浮动机场状态栏重叠。详细规则、资源及验收见 [AIRCRAFT-CABIN.md](AIRCRAFT-CABIN.md) 与 [ART-ASSETS.md](ART-ASSETS.md)。src/core、存档9、数据库1、依赖和离线结算均不改变。

# 参考货物品类展示（2026-09-15）

按用户确认，仅扩充订单名称和美术，沿用现有价格与运输规则。参考 APK 常规货物表的24种通用品类，生成原创水果食品、行李日用品、工业原料和特殊货品图集。普通、快件、冷链、工业服务类别和专用舱限制保持；UI 按服务与订单 ID 稳定映射货名和外观，装卸、刷新、存档、离线不重选。库存物资继续显示真实物资名称，不能伪装成另一种可入库货品。

候运货单显示货名和金额两行，机舱继续显示目的地和金额两行，货名保留在悬停与无障碍说明中。新货物图集原生方向直接显示，飞机与空座椅继续原有镜像。详细品类、服务映射、阶段边界和原创来源见 [ART-ASSETS.md](ART-ASSETS.md)。不新增经营品类字段、供需规则或新收费；src/core、存档9、数据库1、依赖均不变。

# 左向飞机位图与暂用SVG边界（2026-09-15）

所有飞机绘制一律头朝左侧。12个现代机型目录改用原创 1200×540 RGBA `aircraft-{light,regional,heavy}-{passenger,cargo}-v3.png`，覆盖飞机商店、主机场地面、机库和地图加载失败回退；右向生成原图和左向高清源保存在 `art/aircraft-reference-v3/`，来源与提示词见 `art/aircraft-reference-v3/prompts.md`。已无调用方的程序化飞机SVG `PlaneArt` 删除。机场装载页的剖视机舱结构仍是暂时通用SVG，因为要动态排布座席和托盘；后续补位图时必须保持头朝左、真实容量和只读投影。

# 乘客站坐姿美术（2026-09-15）

候运旅客使用新版原创站姿，装入客舱后使用同一人物的坐姿；卸载恢复站姿。6 位人物保持原有订单 ID 对外观编号的稳定映射，采用接近正面、略偏左的朝向。新版图集原生朝左，因此乘客不再水平镜像；新货物方向遵循上方品类增量，飞机与空座椅保持既有方向。替换此前地面与舱内共用站姿图片的约定，资源、透明裁切和验收见 [ART-ASSETS.md](ART-ASSETS.md)。仅改变只读美术投影，经营、存档、依赖及装卸规则不变。

# 机场侧面剖视机舱（2026-09-15）

候运区进一步收紧：普通/短逻辑横屏高度144/104，客货占位96/88，间距6/4；目的地路牌按文字宽度适配并允许长城市名换行。舱内客货使用目的地与实际订单金额两行悬浮文字，移除小牌底板和卸载提示文字，点击卸载保持。替代旧28%高度预算、固定宽站牌及舱内小牌规则，详细尺寸及缩放规则见 [AIRCRAFT-CABIN.md](AIRCRAFT-CABIN.md)。

当前装载展示以 [AIRCRAFT-CABIN.md](AIRCRAFT-CABIN.md) 为准：待运客货留在地面，真实装机订单移入侧面剖视客舱/货舱，点击机内客货卸载。替换此前“装机后仍在地面原位展示”和停靠飞机 PNG 上只叠加容量图标的规则。保留真实订单、现有装卸限制、静默反馈、统一缩放与离线缓存；机体为通用原创示意，旧聚合订单带实际数量，不宣称精确机型内部复刻。核心、经营数值、存档与依赖不变。

# 地图统一 3D 飞机（2026-09-15）

用户选定 A 款 Low Poly Airliner（Mauro3D，CC BY 4.0），用于浏览地图与制定路线的统一飞机展示。该增量替换下文“按机型使用 PNG / 镜像保持机身正立”以及 GLOBE.md 的“不引入 Three.js”限制：PixiJS 继续绘制正射球面与城市航网，独立透明 Three.js 层绘制真实飞机网格，同步相机、尺寸与同一个按需刷新循环。详细资源、朝向、回退、离线与验收边界见 [MAP-AIRCRAFT-3D.md](MAP-AIRCRAFT-3D.md)。经营状态只读，存档与 IndexedDB 版本不变；机场场景、机库和商店仍按实际机型展示既有美术。

# 人才培养与公司纪事（2026-09-14）

当前公司组织与人员经营以 [COMPANY-TALENT.md](COMPANY-TALENT.md) 为准：头像关系树优先的独立工作页、按需招募和档案、固定候选名单、真实经理带教记录、可暂缓公司事务及独立关键纪事。替换旧宽版弹窗和常驻招募/档案布局；存档升级9，数据库仍1，包与依赖不变。冻结 v8 core 后迁移，不更改原资金、合同、在途航班、订单或已开始补能。地图触控、单语言标题、机场代码隐藏、顶部只读点券及直接部署契约保持。

# 帮助、点券图标与球面美术（2026-09-14）

当前帮助页改为四块结构化说明：快速开始、球面地图、经营入口、资源与结算，并保留可直接启动的分步引导。帮助内容只显示当前界面语言，不叠加另一种语言或英文装饰小标题；明确真实订单、最终目的地单次结算、中转、路线预览只读、点券来源、时间事件和最多8小时离线补算规则。该页面只是既有规则说明，不新增指令或经营数值。

顶部点券改用原创金色票券图标，奖杯只继续表示经营中心/成就语义。球面美术在现有正射投影内升级深浅海洋、陆地光照、海岸描边、经纬网和大气边缘；按用户补充要求，不替换或修改球体外的地图背景。航线在大圆方向上使用中段抬升的抛物线高度，飞机标记使用当前机型对应的本地飞机美术，不再使用三角形符号；飞行中机头按屏幕航迹切线指向下一位置，并通过水平镜像保持机身正立。球体背面裁切、缩放与旋转、标签命中、按需重绘、离线运行和核心只读边界全部保持，详见 [GLOBE.md](GLOBE.md) 与 [ART-ASSETS.md](ART-ASSETS.md)。

# 地图浮动导航与触控（2026-09-14）

当前地图浏览布局以 [MAP-BROWSE-CHROME.md](MAP-BROWSE-CHROME.md) 为准：保留顶部资源状态栏，移除其下方返回机场/重复资金/语言设置工具行；底部导航改为左下角独立按钮，不占地图高度，空白与间隙可穿透操作地图。城市标签仅避让实际按钮与提示区域，不再整条避开底部。浏览与制定路线均移除放大/缩小按钮，保留双指、滚轮、键盘缩放；“隐藏其他飞机”仅保留在制定路线。经营规则、存档、依赖与现有比例缩放不变。

# 公司组织独立入口与布局（2026-09-14）

公司组织从经营中心二级页签提升为底部独立普通导航入口；普通入口顺序为地图、机场装载、机场目录、机队管理、飞机商店、公司组织、经营中心，制定路线仍只在机场页作为最右侧高优先级入口。公司组织打开独立宽版页面，经营中心不再保留同名页签。页面采用组织概览、招募区、组织树与人员档案的独立信息层级，每个区域只显示当前界面语言的主标题，不再显示英文装饰小标题。机场缩写继续作为核心、存档和搜索匹配的稳定 ID，但不出现在机场标题、目录卡片、目的地站牌或城市选择项等可见界面。导航提升及美术布局不修改核心命令、经营数值、存档版本或结算。

# 公司组织树（2026-09-14）

当前人员经营以 [COMPANY-ORGANIZATION.md](COMPANY-ORGANIZATION.md) 为准：统一飞行与地勤员工身份、经理汇报关系、预付合同、培养与履历。替换仅飞行员名单、固定120秒补能及存档7的阶段限制，存档升级为8，数据库仍1。没有经理或地勤仍可进行原有手动/自动运输和基础补能；已开始补能截止时间与所有历史航班保持。顶部只读点券、装卸静默及机场核心体验不变。

# 启动、默认地图、航行动画与本地化（2026-09-14）

当前入口、导航顺序、在途视觉和中英文契约以 [START-MAP-I18N.md](START-MAP-I18N.md) 为准。首次进入显示飞机航行开始场景，进入后默认打开地图；地图为底部最左入口。浏览地图保留普通导航，只有制定路线页隐藏全局导航。动画纯展示且遵循减少动态效果，不能参与时间或收益结算。语言偏好独立于存档，核心及旧存档校验不依赖翻译；本轮不提升存档版本，继续使用公司组织增量定义的存档8。

# 地图浏览与制定路线分离（2026-09-13）

“航线地图”入口重命名为“地图”，只用于旋转缩放航网、查找城市、查看机场详情及解锁/升级城市。顶部显示已解锁城市数和可用资金，城市点击只打开详情；解锁成功后保留详情，返回地图保持镜头，不追加路线、不移动飞机。地图不显示目的地/预计时间/净利润/成本读数、草稿、航程圈、机上客源提示或检票起飞按钮。飞行和空机场场景均可进入，返回保持进入前的飞机或固定机场。

“制定路线”继续是机场右下角的发航入口，使用当前飞机，依次选择城市追加最多12段；路线明细、能量/收益预览与检票起飞仍沿用原规则。选路过程中点未开放城市仍可解锁，成功才追加该站；取消不追加。机场详情的“安排飞机飞来”明确进入制定路线，仍以真实飞机位置为起点。

这替换下文、GLOBE.md 与 ROUTE-DISPATCH-REFERENCE.md 中两个入口共用选路行为的约定；球面渲染、离线城市搜索、全局告警及比例缩放契约保持。两种入口各自初始化浏览状态/路线草稿；浏览时从不调用派航或规划命令。任务文案同步为“制定首航路线”。经营数值、存档和依赖不变。

客货区按 [TASKS-FLEET-LOADING.md](TASKS-FLEET-LOADING.md) 的两行展示精简重复状态、动作与类型文字。回归覆盖浏览/规划的点击差异、解锁后不产生草稿、取消与刷新恢复、空机场和飞行状态、触控及无WebGL回退，同时保留既有经营与离线验收。

# 任务与机队入口整合（2026-09-13）

当前任务入口、航班/改装归属及客货三态展示以 [TASKS-FLEET-LOADING.md](TASKS-FLEET-LOADING.md) 为准，替代下文和 AIRPORT-REFERENCE.md 的零散快捷入口及无底色标签规则。仅改 UI 只读投影与交互，能量、收益、核心命令、存档和比例缩放契约不变。

# 比例适配与 UI 缩放（2026-09-13）

显示契约以 [DISPLAY-SCALING.md](DISPLAY-SCALING.md) 为准：同可用宽高比与同UI缩放使用相同逻辑布局；取代按设备像素区分手机/桌面及44实际像素的限制。默认100%，允许75%–150%，不修改经营状态或存档。

# 单机经营扩展（2026-09-13）

当前规则以 [OFFLINE-CAREER.md](OFFLINE-CAREER.md) 为准，替换此前基础数值、仅三级改装、无独立人员及无库存的限制。保留冻结旧存档校验和在途收益。本轮明确不实现宝石币种。

# 当前增量：球形航网与世界机场

## 机场场景布局修订（2026-09-12）

本轮在同步主分支 `7173d0a` 后，按用户补充要求调整机场场景：客货直接站在候运地面，按目的地分组、每组左侧共用一个独立站牌，客货排在右侧，单体头顶不再重复标注目的地。删除“候机大厅”工具行，将待运与当前飞机已装机客货合并在同一组展示；点击装机后原位显示“已装机 · 点击卸载”，可再次点击卸载。点击站牌可同目的地装机，分类与列表切换控件移除。“制定路线”完整放入右下角底部导航，队列恢复全宽；机体客货示意始终显示，删除显示开关。场景不再重复绘制机场名称，顶部机场信息保留。移除底部起航引导按钮及说明，以左上首航任务和任务内下一步操作引导玩家；进度由真实装载、飞行、领奖状态决定。新单体订单不显示数量胶囊，旧聚合订单保留实际数量。详细交互见 [AIRPORT-REFERENCE.md](AIRPORT-REFERENCE.md)。不修改核心经营、存档或独立发航页。

## 本地美术资源增量（2026-09-12）

机场地面背景与客货卡改用原创生成的本地位图，资源规范和阶段边界见 [ART-ASSETS.md](ART-ASSETS.md)。透明素材按本轮要求先生成纯绿背景，再抠图、去绿边并输出 RGBA PNG；保留原图与提示词。旅客外观按订单 ID 稳定选择，所有货单仍只使用普通货箱，不新增职业、货种或收益规则。第二批按用户“替换美术资源”要求，将主场景飞机替换为地面/飞行两种客机 PNG，真实客货数量与容量仍由只读 SVG 叠层绘制；当前为通用机体示意，未逐机型绘制。新增12个导航/状态图标、飞行员头像和4种地勤装饰，图标不改变按钮名称和命令，地面装饰不接收点击且飞行时隐藏。机场名称仅在顶部真实状态区显示。图片随 PWA 预缓存，不新增在线依赖，不改变存档、核心命令或结算。

球面投影、首批50座全球机场、旧存档迁移和新增校验以 [GLOBE.md](GLOBE.md) 为准。本条替换下文及MAP-LAYOUT.md中的平面地图、限定12机场和“存档保持5 / 核心不改”范围限制。包版本和依赖不变，存档格式6；不更改后台结算、真实订单、能量费用和独立发航页布局。其余经营规则继承下文。

---

# 中华航空 · v0.5 基础能量预算

本轮从界面补齐转向实际运营约束：按已读取的2016年攻略记录“飞行每分钟消耗1点”的比例，新增能量预留、不足停止与地勤补能。并未完成原作全部数值复刻。

## 设计冲突与继承

旧版只检查运营资金、航程和机场等级，可以在有供给时一直自动飞行。本轮明确替换这一无限连续运行规则；除 [ENERGY.md](ENERGY.md) 的能量条款外，完整沿用 [ARCHIVE-V0.4-ROUTES.md](ARCHIVE-V0.4-ROUTES.md) 及其引用的人员、机场浏览、航班运行和经济规则。不能把界面禁用当作核心已实施限制。

默认240点容量取自旧攻略的一个示例，并非确认所有原版车型都是240点。120秒免费地勤补满、按秒预留和取消规则是航空化暂定设计；没有查明原版恢复时点、费用及不足一分钟的舍入，必须在界面和数值档案中明确，不擅称等价。暂不添加未经核实的原版付费货币。

## 运行与兼容

包版本0.5.0，存档格式5，IndexedDB结构仍1；依赖版本不变，lockfile只更新根包版本。每架飞机有独立可用能量、当前航段预留量和补能截止时刻。唯一写入者仍为src/core；UI只显示数据并发送命令，不用动画或计时器回调扣能。

手动、自动、多段全部经过同一depart入口检查并预留。余额不足时当段不能起飞或扣钱；已有在途航班正常抵达。后续计划或自动值勤遇到不足，在实际机场停止，保留已装载订单。补能需完成周转、停止值勤和取消后续计划；完成前不增加能量，取消也不补。补能不是飞行，不产生运输收入、不消耗或清空客货。

冻结v4校验器和catalog-v4。旧版v1—v4先严格验证再迁移到5；旧资金、订单、改装、时刻和已锁定航班不重算，每架旧机赋满初始预算，当前旧航班豁免追扣。只有今后起飞才预留。v5读档不能重复赠能，非法或缺少字段拒绝且不覆盖有效进度。

## 验收与交付

验证整秒耗能、精确余额边界、命令原子性、预览只读、空飞与改装速度、自动缺能停止、多段中转保留、补能时点/取消/刷新/离线、旧存档兼容及损坏字段。保留所有旧测试的业务断言，只调整当前存档版本和新增字段的迁移投影。新增桌面、844×390、667×375补能操作与资金不变验证。

所有合并仍必须通过类型检查、单元、根/子路径构建和零重试浏览器测试，查看截图后通过 PR squash 合并。合并后的 Pages 流程只安装锁定依赖、执行一次子路径生产构建并部署；日常修改不生成源码或静态 ZIP，离线交付包仅在用户明确要求时手工生成。没有部署回执不宣称上线。不得覆盖并行工作、修改仓库可见性、购买服务或上传密钥。真实设备与像素级原作验收仍未完成。

## 保留并行合入的城市选路规则

主分支abc4d17已合入按城市点击顺序规划路线的规则，本轮完整保留，不恢复单段/多段切换或航线建设费。城市点击顺序形成最多5段，解锁城市且满足现有等级/航程/状态/资金与新增能量要求即可起飞。routes只是实际运营/兼容记录，预览和失败派航不能写入记录。

v4冻结校验基于该最新路线规则，既接受更早的已购买routes，也接受仅记录首段、尚未记录未来航段的合法v4计划；迁移不加建未来航线，不补扣建设费或旧航班能量。v5重新起飞才预留能量。新能量E2E使用统一城市选择，不依赖已移除的模式或开通按钮。

## 独立发航界面（取代常驻城市工具与全局导航）

本轮精简布局以用户提供的发车截图与已确认方案为基准，详细规则见 [ROUTE-DISPATCH-REFERENCE.md](ROUTE-DISPATCH-REFERENCE.md)。此前常驻资源栏、主导航、城市选择/详情/自动往返/能量工具条的条款被替换；经济与存档约束全部继承，不做数值变更。

默认只显示目的地、预计时间、净利润和成本四项读数；地图直接延伸到页面底部。城市选择由顶部目的地按需打开，路线/能量/自动往返由“查看路线”按需打开。左下缩放与路线后退/撤销/取消/查看，右下唯一“检票起飞”；当飞机在途，返回按钮明确叫“返回航班”。错误和存档冲突保留，正常通知不占发航地图。

取消返回进入前的实际飞机或固定机场，不瞬移、不卸货、不撤销已保存的解锁，也不取消在途航班。地图保持挂载，弹窗不重置草稿和相机。隐藏其他飞机只改变渲染；航程圈围绕末站提示但所有可飞判断仍由核心执行。

三种视口的地图高度至少为85%，主要点击区不小于44像素。回归测试改用新的真实入口，经营/存档原断言保留。包0.5.0、存档5、核心/运行控制器/持久化/依赖不改。通过截图与全量测试后按既有约定打包、squash合并、部署。

## 乘客目的地地图标注

规划路线时，发航地图读取当前飞机的已装载订单，并仅对 `kind === passengers` 且 `location` 为当前飞机 ID 的订单按目的机场汇总。在对应机场旁显示高对比“乘客 N”徽标，N 为该目的地机上旅客人数；货物、其他飞机订单和仍在候机大厅的订单不参与标注。

该标注是纯 UI 只读投影，不修改订单、草稿、航程判断、派航条件或存档，也不新增后台状态。装卸或交付造成机上乘客变化后标注随最新快照更新；当前飞机没有已装载乘客时不显示任何乘客目的地徽标。

## 单体客货与小容量初始机（2026-09-12）

本轮按《中华铁路》“初始只有一节车厢、可通过强化增加车厢”的公开开发者回复校正开局节奏。没有查到原作初始车厢的精确人数/货量，因此只继承“小容量起步、逐个选择客货、再靠升级扩大容量”的交互原则，不宣称数值一比一。

新生成的需求订单固定 `amount = 1`：旅客卡代表1位旅客，货物卡代表1吨货物。候机大厅和机上清单不再新生成15/20人聚合旅客组。`load`、`load-destination`、中转、收益和统计仍以真实订单为唯一来源，不能因为界面刷新或飞行自动制造客货。

新开局的 AC0001 改用仅用于初始赠送的“云雀 6”：6个旅客位、1个货位，航程/速度/成本保持云雀系列入门级节奏，可继续做容量改装；商店仍保留原9种可购机型，云雀70不被删除。这样首架飞机的每个容量位都对应少量可见卡片，避免70座飞机配单体旅客造成大量无意义点击。

存档格式仍为5，不改字段。旧v1—v5存档和已在途航班必须继续按原模型与锁定收入验证；旧存档中已经存在的15/20人等聚合订单不拆分、不改ID、不重算报酬，直到交付或自然过期。新补充的客源才使用单体订单。旧存档中的云雀70也不自动缩容，避免合法机上载荷因本轮规则变化而变成非法。

验收增加：新局12张候运卡全部为单体订单；AC0001容量为6人/1吨；同目的地装载恰好填满两类容量并拒绝继续超载；刷新后仍保持该载荷；v1固定在途聚合样本继续按原收入迁移和结算；商店仍为原9种机型。浏览器截图应能看出旅客卡只画一位旅客、初始机客舱只显示少量容量位。
