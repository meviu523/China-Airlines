# 跨页面稳定导航（2026-09-15）

## 当前规则

按照“稳定不同页面下导航栏按钮位置、内部按钮左对齐”的要求，替换机场/公司页居中、地图页单独左下定位的布局差异，也替换“制定路线使用自动左边距推到最右侧”的旧规则。此增量优先于旧机场参考、公司组织、地图浏览文档中的导航几何描述；入口功能与顺序不变。

普通入口固定为地图、机场装载、机场目录、机队管理、飞机商店、公司组织、经营中心，从同一左侧起点按固定间距排列。制定路线只在机场页出现，紧接经营中心之后；它的显隐不改变前七个按钮的坐标、宽高或间距。选中、悬停、按下只改变视觉反馈，不平移按钮。

所有页面使用同一套逻辑尺寸：常规水平内边距12、垂直内边距8、导航高度94、间距8、普通按钮84×78、制定路线118×78；逻辑宽度不超过950的横屏普通按钮宽72、制定路线宽108。逻辑高度不超过600的横屏统一改为水平内边距8、垂直内边距4、导航高度60、间距5、普通按钮59×52、制定路线90×52。尺寸切换只依据现有逻辑视口，不依据设备类型或物理屏幕大小。

机场与公司组织页保留导航的固定布局高度，避免遮挡客货队列和人员内容；地图导航使用相同框体与按钮坐标，脱离布局流且完全透明，不减少地图画布高度。地图容器、内边距和间隙不接收指针，只有实际按钮接收点击；标签继续按真实按钮矩形避让，不能恢复整条底部禁区。制定路线仍隐藏全局导航。

游戏最外框已取消，因此制定路线与路线规划页“检票起飞”均直接以游戏内容边缘为基准，不再保留历史4px/短横屏2px的外框补偿。两者的右边缘、底边和高度必须保持完全一致；去掉外描边不能造成主操作按钮内缩。

## 实现边界

`src/ui/navigation.css` 为导航几何的统一入口，由 `src/main.tsx` 在既有页面样式之后载入。`map-browse.css` 仅保留浏览页搜索按钮样式，避免重复定义地图专属导航坐标。保留既有图标、单语言标题、角色入口、模态窗口、缩放、触控和离线契约。不更改 core、命令、资金、航班、订单、存档、数据库版本或依赖。

## 回归验收

`e2e/navigation-stability.spec.ts` 比较七个普通入口的实际坐标及宽高，覆盖机场→地图→公司组织→地图→机场，以及从机场进入公司组织、打开关闭目录/机队/商店/经营中心、按下选中按钮、制定路线退出和刷新恢复。覆盖桌面、两种手机横屏、窄横屏、75%/100%/150%缩放及中英文；同时检查左对齐、固定间距、制定路线紧随普通入口、按钮均在视口内和地图间隙命中画布，并验证制定路线与检票起飞共享相同的右边缘、底边和高度。

既有 `map-floating-navigation.spec.ts` 继续验证全高地图、底部标签避让、拖动/双指/滚轮和各按钮点击；其他浏览器用例继续覆盖经营、导入导出和离线启动。根路径及 `/China-Airlines/` 部署子路径均由原有 Verify 工作流验收。

当前执行容器无法解析 GitHub 域名，不能克隆仓库或安装完整依赖，因此完整类型检查、单元测试、构建与浏览器结果以本次 PR 的 Verify 回执为准；不将未运行检查或前一版本结果当成本次通过。合并后沿用既有 Pages 直接部署，不另外生成源码或静态 ZIP。

## 高度与刷新回归修正

水平与垂直留白分别定义，保留原有94/60逻辑像素的导航占用高度，不能为了统一坐标而挤压公司组织树、机场客货或场景。保留原有经理节点完全可见和底部按钮边距断言。货品离线回归在卸货后先确认机上订单消失且候运名称恢复，再刷新并核对同一订单及资金；不以立即刷新中断尚未完成的异步命令，不增加盲等或重试。