# 客货下方信息与全机队容量美术（2026-09-16）

机内目的地与金额固定在人物/座椅、货物/托盘正下方居中显示，不再使用右侧40%文字栏。视觉图层仍裁切在真实舱口内，透明文字和点击区可延伸到地板下沿；相邻舱室按实际渲染边界检查，不能遮挡另一订单。金额直接读取 `order.reward`，不重新计价。

信天翁客货型样机确认后，13架现役飞机全部接入v5独立源图。每架客舱窗位、货舱分段及可摆放锚点都与该机基础客货容量一致；容量升级与调校增加的真实容量继续分页，不改写底图。资源、提示词、参考和透明遮罩处理见 `art/aircraft-capacity-v5/prompts.md`。本轮只在机型目录切换美术版本，不修改 `src/core` 的经营状态、命令、订单、存档9、数据库1或结算。

本地验收记录：美术注册13/13，Vitest 31个文件、511项通过，TypeScript通过，根路径与 `/China-Airlines/` 子路径生产构建通过，Chromium Playwright 225/225通过。真实iOS Safari与真实手机GPU不在本轮本地验收范围内；PR双路径门禁及Pages部署以GitHub工作流结果为准。

# 独立机型双层美术与旧机型退役（2026-09-16）

本增量按用户确认替换下方历史约定中的“三档机体共用”“客货舱左右并排”“历史机型继续使用和迁移”。当前仅保留初航号及雨燕、苍鹭、信天翁、极光各客运/货运/客货型，共13种，每种都有独立生成的剖面与近侧外观资源。

飞机由两层组成：第一层为完整剖面机身、远侧结构及一直挂载的动态机舱；第二层为近侧蒙皮、机翼和发动机。查看外观/查看机舱仅切换第二层可见性，并同步屏蔽被遮挡机舱的键盘与点击操作，不重新创建订单、座位、人物或分页。商店、机库、装配和地图位图回退使用同一对图层合成的该机型独立外观。地图正常3D模型沿用已确认的A款，这批资源用于2D场景与位图回退。

客货型与初航号的客舱在上、货舱在下，分配舱高56%/44%；纯客、纯货机使用一个完整舱室。各机型独立标定剖面区域与近侧外壳位置。壁面取自各自剖面资源；地板下方45%保持与坐垫平面平行的投影，保留无扶手座椅、椅脚与人物臀部/脚底、货物/托盘接触锚点。实际容量、真实订单、卸载和有限可见锚点分页保持；文字预留独立底部区域。

旧机型及兼容支持移除：当前机型目录不含历史机型，移除其旧能耗、自动值勤豁免、改装和残值分支；存档v1–v6不再导入，v7–v9只有现役机型可恢复。导入前检查机队、仓储、博物馆中的机型，发现退役或未知机型即拒绝，绝不自动换机、删机、缩容或覆盖主存档和备份。v7/v8现役存档仍先经各自冻结校验器再迁移，冻结代码仅用于这些版本的严格校验，不作为旧机型运行或导入入口。已有磁盘记录无法恢复时继续展示恢复入口，不自动重开。

游戏存档仍为9，IndexedDB结构仍为1；无新增依赖，经营数值及离线结算不变。完整资源与生成来源见 `art/aircraft-layers-v4/prompts.md`，坐标见 `src/ui/aircraft-layer-layouts.json`。本地验收见下方独立机型增量记录；PR与Pages交付结果以对应工作流回执为准。

## 独立机型增量本地验收（2026-09-16）

- 类型检查、478项单元测试、根路径生产构建、全量与运行依赖审计通过；构建仅保留既有超过500 kB分包提示。
- 首轮全量浏览器212/216：三项确认机内已装机客货信息区只有28逻辑像素、违反44像素命中契约，一项为测试把Playwright假时钟回退到过去。修复信息区与地板预留后，机舱/3D/装载定向46/46通过。
- 第二轮全量215/216的唯一失败来自测试在离线卸载后未等待控制器发布已提交状态；补充等待 `data-load-state=waiting` 后，该用例连续三轮6/6通过。最终全量浏览器216/216通过，零重试。
- 已覆盖桌面、两种手机横屏、75%–150%缩放、竖屏提示、装卸、刷新、导入导出、退役存档拒绝和离线启动。本轮为Linux/Chromium自动验收，真实手机与iOS Safari仍未验收；根路径与 `/China-Airlines/` PR验收、合并后的Pages部署及公开页面检查以实际工作流和浏览器回执为准。

# 地板与坐垫平行透视（2026-09-15）

保持侧面剖视及原舱壁、舷窗，只修改地板俯视投影。以现有v3坐垫的宽边和纵深边方向为基准（画布向量385/45与148/-79），地板两组接缝分别朝远处消失点收敛，客货舱使用同一投影参数。地板仍占舱高下方35%，人物、座椅及货物锚点保持。

替换此前地板图片纵向放大588%/476%的实现：仅从原图取无轨道材质，地板接缝和货舱系留轨道用本地SVG投影到逻辑像素，避免客货舱宽度变化拉歪透视；不旋转整间机舱，不采用本轮生成但未确认的背景图。此为美术投影调整，经营、存档、容量、装卸和依赖不变。

## 地板投影本地验收

- 类型检查、原471项单元测试及新增2项投影测试通过；根路径与 `/China-Airlines/` 生产构建通过，保留既有分包体积提示。
- 根路径相关35项浏览器流程首轮34通过、1项因截图文件占用写入失败；更换截图文件名后该项单独复验通过。部署子路径机舱29/29通过，无重试或跳过。
- 覆盖桌面/两种手机横屏、75%–150%缩放、竖屏提示、刷新、导入导出、离线启动和装卸；新增验证地板两轴等比例显示、原错误透视地板已移除。未运行全仓库浏览器套件或真实手机硬件验收。
- 最终效果：`artifacts/cabin-floor-1440-100.png`、`artifacts/cabin-floor-667-150.png`。未提交、推送或部署。

# 座椅透视与二维接触点（2026-09-15）

按参考图及补充要求，座椅改为接近正面并进一步朝左约25度的原创无扶手座椅，补齐可见金属前后椅脚；货架/托盘采用浅俯视、可见窄右侧面的透视。运行资源为 `cabin-seat-v3.png`、`cabin-pallet-v3.png`，来源和提示词见 `art/cabin-layered-v3/prompts.md`。

本增量替换此前“人物与座椅同高”和独立前扶手图层的约定。6位乘客分别记录归一化臀部x/y与脚底y：人物臀部同时对齐坐垫承重点的两轴，脚底与椅脚落在同一地板基准；缩放前共同约束人物和座椅边界。货物底部对齐托盘顶面，文字仍在独立下方区域。只改美术投影，不修改经营、容量、订单、存档或结算。

## v3 本地验收

- 类型检查、471项单元测试、根路径与 `/China-Airlines/` 生产构建通过；保留既有500 kB分包提示。
- 根路径机舱、美术及游戏流程36/36通过；部署子路径机舱29/29通过，均无重试或跳过。覆盖三种横屏尺寸、75%–150%缩放、竖屏提示、装卸、刷新、导入导出与离线启动。本轮未重复全仓库浏览器套件，未验收真实手机硬件。
- 已查看桌面与667×375高缩放截图，确认无扶手椅、金属椅脚、二维臀部/坐垫接触和货物落架显示；效果位于 `artifacts/cabin-1440-100.png`、`artifacts/cabin-667-150.png`。
- 修改保留在当前功能分支，未提交、推送或部署；原有其他工作区修改保留。

# 分层位图与锚点增量（2026-09-15）

本节替换下文暂用通用SVG机体、CSS座椅/托盘与flex排列的美术实现；下文装卸、聚合数量、分页、只读及经营规则继续适用。

- 原创加厚左向机身三档：轻型、中型、大型；12个现代机型对应纯客、纯货、混合布局。远程与洲际共享大型机体；历史机型按美术/航程选择示意档位，不更改核心型号或容量。
- 分层顺序：机体PNG → 客舱/货舱内部PNG → 座椅/托盘PNG → 真实客货PNG → 舱壁边框。标签仍为正常方向的DOM文字；边框与美术不拦截点击。标题/独立分页控件置于剖面下沿，避开上方机场状态条，保持44逻辑像素按钮。
- `src/ui/cabin-art-layout.ts` 记录原画布尺寸、舱内矩形、客货分区比例、每页锚点上限和左向标记。锚点记录编号、类型、局部归一化横坐标/底线、命中宽度、共用落地基准及层级；家具/客货尺寸通过同一几何函数计算。舱内矩形负责从画布到场景映射，锚点在各舱局部坐标内定位。
- 每96逻辑像素设置一个可用锚点，每档最多8/10/12个；宽度不足时至少保留一个。每舱按现有 `createdAt + id` 排序填入当前页锚点；空闲容量显示座椅/托盘。分页、窗口缩放、新装载自动定位、聚合订单真实amount与紧凑排列沿用原规则，锚点不存入存档。
- 底图墙面与地板分段显示，地板从舱高65%开始，统一落地点优先位于78%，短舱在地板范围内调整以保留独立44高文字带。人物和座椅使用相同高度、相同脚底线；托盘宽度至少为所承载货物宽度的1.18倍（受锚点宽度限制），货物底部落在托盘顶面。文字单独放在落地点之后，不再用文字预留空间抬高人物或托盘。
- 展示容器普通布局目标高430、短逻辑横屏360，受现有场景实际高度限制。加厚机体与内部剖面放大，人物区域不再扣去44高的舱标题。小屏通过减少每页锚点保障可点击区域，不扩张经营容量。
- 开发服务器添加 `?cabinAnchors` 可显示锚点编号与命中边框；生产版本不启用调试叠层。正式界面不显示实现信息。
- 原图、生成提示词保存于 `art/cabin-layered-v1/`，7张运行PNG存入 `public/art/`，通过已有PWA预缓存离线使用。仅参考本地APK `ui/pos.plist` 的 `body/passenger/cargo` 分离方式，未复制其素材、坐标或代码。

## 本增量验证（2026-09-15）

- 最终类型检查、470项单元测试、根路径及 `/China-Airlines/` 生产构建通过；构建保留既有500 kB分包提示。
- 完整浏览器回归执行203项：200通过，3项发现图像容器与文字带边界重叠。随后保留真实地板接触并修正图像容器及44高文字带，根路径相关40项全部通过（机舱29、飞机美术1、离线素材1、装载/任务9），无重试或跳过；未在这次最后修正后重复全部203项。
- 部署子路径在相同最终代码上复验上述40项，40/40通过，无重试或跳过；报告为 `artifacts/artifacts/cabin-browser-subpath.json`。首次子路径预览仅绑定localhost导致127.0.0.1就绪探测超时，改为明确绑定127.0.0.1并使用独立端口后正常运行。
- 已查看桌面、667×375高缩放及多机型截图，座椅与人物同高共用脚底线，货物小于托盘宽度并落在托盘顶面。浏览器检查包括地板范围、素材完整加载、点击卸载、翻页、全机型、75%–150%缩放、刷新、导入导出、英文及离线运行。真实手机硬件未验收。
- 当前功能分支 `feat/layered-aircraft-cabin`，本次未提交、推送、合并或部署。工作区原有其他修改保留。

# 机场装载：侧面剖视机舱（2026-09-15）

机场装载页使用原创侧面剖视机体，客舱内排列旅客与座椅，货舱内排列货箱与托盘。继承《中华铁路》车厢装载的空间操作方式，不复制其素材或代码，不宣称飞机内部结构与原作一比一复刻。

装载场景的机头、地面与舱内客货统一采用朝左的展示方向，空座椅随之转向。乘客改用原生接近正面、略偏左的新版站姿/坐姿图集；24种货物采用各自原生图集方向，二者均不再镜像。飞机和空座椅继续仅镜像美术图层。目的地、数量、价格、操作文字及翻页顺序保持正常阅读方向；不改变地图飞机按航迹定向的规则。候运客货脚下使用柔和椭圆阴影，移除白色描边椭圆底座；价格/物资名称直接悬浮，不再显示金额底板及装机/不能装载原因文字。真实禁用状态、无障碍原因及容量判断保留。移除机场牵引车、行李拖车、地勤人员和路锥装饰。

## 替换的约定

本增量替换 DESIGN.md、AIRPORT-REFERENCE.md、TASKS-FLEET-LOADING.md 中“已装机订单仍在地面原位展示”和“停靠飞机只显示容量图标”的条款。ART-ASSETS.md 中机场按实际机型显示 PNG 的规则在停靠装载场景改为通用剖视机体；实际机型名、容量仍读真实飞机。地图 3D 模型、飞行场景、机库与商店的既有美术规则保持。

## 装卸与舱内位置

- 地面仅显示当前机场待运订单，按目的地站牌分组。点单个客货或站牌，调用原有 `load` / `load-destination` 命令。
- 保存成功后，同一订单从地面移至当前飞机的客舱或货舱。旅客从站姿切换为同一人物的坐姿，卸载后恢复站姿；人物编号、服装、货物类型和订单 ID 保持。容量为0的舱不显示。没有订单时只显示空座椅/空托盘，不能生成装饰乘客或货物冒充载荷。
- 点击舱内人物/货箱，通过原有 `unload` 命令返回地面目的地分组。补能、周转、自动值勤、剩余计划、候运区满和保存中均沿用既有禁用条件。客舱满不影响可用货位，反之亦然。成功装卸继续静默，不新增弹窗。
- 地面旅客只显示悬浮价格；普通货单显示具体货名和价格两行，贸易物资保留真实物资名称。舱内客货下方以两行悬浮文字显示目的城市和实际订单金额，具体货名在悬停与可访问名称中保留，不显示卡片底色、边框或卸载提示文字。点击客货卸载保持，操作与限制保留在可访问名称及悬停说明中。金额直接读取 `order.reward`，不另外计算价格。旧聚合订单以一个带实际人数/吨数的群组显示，真实容量按 `amount` 扣除，不拆单、不改价格。
- 每舱独立分页，页内为按创建时间及订单 ID 稳定排列的已装订单，其后为空位。页面大小随逻辑宽度变化；卸载后可紧凑排列。新装入订单自动翻至包含它的一页，切换飞机或刷新从首页展示。不写入座位号、分页或额外存档字段。
- 翻页仅改变展示；支持按钮及方向键、Home、End。查看机上客货入口回到舱内首页并移动键盘焦点；卸载后焦点保留在对应舱。使用已有统一 UI 缩放。

## 候运区紧凑布局（2026-09-15）

按用户要求缩小地面客货区高度和间隔：普通逻辑布局高度由194降至144，短逻辑横屏（高度不超过600）为104，默认100%缩放占页面高度20%，150%缩放约22%。释放的高度由上方机场场景使用。单个客货占位由130/118缩至96/88，组内间距由12/6缩至6/4；同目的地仍按真实订单稳定排列，不改变装卸或滚动交互。

目的地路牌按文字自然宽度显示，普通布局宽64–160、短横屏宽56–144，长城市名自动换行，文字居中；取消固定124/100宽度。路牌与队列翻页按钮保持至少44逻辑像素高，长名称与金额不得裁切。以上尺寸继续使用统一UI缩放，不按实际设备像素分套布局。此规则取代旧机场参考文档的28%客货区及桌面维持原比例约定。

## 经营与阶段边界

`src/core` 仍是经营状态唯一写入者，容量、收益、时间事件、最多8小时离线补算、时间回拨规则均不改变。包版本0.8.0、存档9、IndexedDB结构1、依赖不变。所有素材与程序随 PWA 离线缓存。

本阶段是可操作的二维剖视示意，不提供走动登机、拖放选座、真实机型座位布局或碰撞模拟。剖视机体结构当前是暂时通用SVG；后续绘制位图时必须头朝左并保持动态舱位和真实容量投影。飞行中继续显示已有航班场景和只读容量示意；到达最终目的地的订单按核心结算后消失，中转订单继续显示在机内。

## 验收

投影测试覆盖空舱、装卸身份与美术一致、跨飞机隔离、纯客/纯货、扩容分页、旧聚合数量和快照只读。浏览器覆盖桌面及两种手机横屏、75%–150%缩放、机内边界与点击、补能禁用、翻页、键盘、刷新、导入导出、英文及离线装卸；保留竖屏提示、飞行与结算回归。实际运行结果另行记录，不以此清单冒充通过。

## 本地验证记录（2026-09-15）

- 类型检查、根路径及 `/China-Airlines/` 生产构建通过；构建仍提示部分既有包超过500 kB。
- 单元测试27个文件、464项通过。
- 机舱主功能全量浏览器185/185通过；首轮失败的原位坐标、全清单可见数量、重复飞机标记均已修正后复验。
- 随后按用户补充调整朝向、地面阴影、金额底板/原因文字和地勤装饰，相关浏览器52/52通过；最终舱标题避让微调后，机舱专项在根路径与部署子路径各17/17通过，无重试或跳过。未在最后这组纯展示调整后再次运行全部185项。
- 已查看桌面及手机横屏截图，覆盖75%–150%缩放。截图位于 `artifacts/cabin-1440-100.png`、`artifacts/cabin-667-100.png` 等；真实手机硬件验收未执行。
- 当前为功能分支上的本地修改，尚未提交、推送、合并或部署。
