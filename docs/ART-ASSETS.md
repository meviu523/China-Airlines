# 容量化飞机剖面全机队 v5（2026-09-16）

信天翁客货型样机确认后，初航号及12个现代机型全部重绘为独立v5容量源图。客舱窗位、货舱分段与目录基础容量逐架对应。源图、完整提示词集合与参考边界见 `art/aircraft-capacity-v5/prompts.md`；构建继续从各自1536×1024图集注册1536×590剖面、近侧外壳与合成外观。生成源的RGB/RGBA差异由本地解码器统一，外部低透明光晕由各自v4同轮廓alpha遮罩恢复；内部绘制和色彩仍来自v5源图，四角保持透明。

Virtual Aircraft Museum 只用于机型分类和通用结构研究，不下载或接入其图片、商标、涂装和线稿。v4源图只保留为逐机透明轮廓遮罩及历史生成依据，39张旧v4运行图已删除，运行目录统一引用v5。

# 独立机型双层美术与旧机型退役（2026-09-16）

本增量按用户确认替换下方历史约定中的“三档机体共用”“客货舱左右并排”“历史机型继续使用和迁移”。当前仅保留初航号及雨燕、苍鹭、信天翁、极光各客运/货运/客货型，共13种，每种都有独立生成的剖面与近侧外观资源。

飞机由两层组成：第一层为完整剖面机身、远侧结构及一直挂载的动态机舱；第二层为近侧蒙皮、机翼和发动机。查看外观/查看机舱仅切换第二层可见性，并同步屏蔽被遮挡机舱的键盘与点击操作，不重新创建订单、座位、人物或分页。商店、机库、装配和地图位图回退使用同一对图层合成的该机型独立外观。地图正常3D模型沿用已确认的A款，这批资源用于2D场景与位图回退。

客货型与初航号的客舱在上、货舱在下，分配舱高56%/44%；纯客、纯货机使用一个完整舱室。各机型独立标定剖面区域与近侧外壳位置。壁面取自各自剖面资源；地板下方45%保持与坐垫平面平行的投影，保留无扶手座椅、椅脚与人物臀部/脚底、货物/托盘接触锚点。实际容量、真实订单、卸载和有限可见锚点分页保持；文字预留独立底部区域。

旧机型及兼容支持移除：当前机型目录不含历史机型，移除其旧能耗、自动值勤豁免、改装和残值分支；存档v1–v6不再导入，v7–v9只有现役机型可恢复。导入前检查机队、仓储、博物馆中的机型，发现退役或未知机型即拒绝，绝不自动换机、删机、缩容或覆盖主存档和备份。v7/v8现役存档仍先经各自冻结校验器再迁移，冻结代码仅用于这些版本的严格校验，不作为旧机型运行或导入入口。已有磁盘记录无法恢复时继续展示恢复入口，不自动重开。

游戏存档仍为9，IndexedDB结构仍为1；无新增依赖，经营数值及离线结算不变。完整资源与生成来源见 `art/aircraft-layers-v4/prompts.md`，坐标见 `src/ui/aircraft-layer-layouts.json`。本地验收记录见 [AIRCRAFT-CABIN.md](AIRCRAFT-CABIN.md)；PR与Pages交付结果以对应工作流回执为准。

# 地板与坐垫平行透视（2026-09-15）

保持侧面剖视及原舱壁、舷窗，只修改地板俯视投影。以现有v3坐垫的宽边和纵深边方向为基准（画布向量385/45与148/-79），地板两组接缝分别朝远处消失点收敛，客货舱使用同一投影参数。地板仍占舱高下方35%，人物、座椅及货物锚点保持。

替换此前地板图片纵向放大588%/476%的实现：仅从原图取无轨道材质，地板接缝和货舱系留轨道用本地SVG投影到逻辑像素，避免客货舱宽度变化拉歪透视；不旋转整间机舱，不采用本轮生成但未确认的背景图。此为美术投影调整，经营、存档、容量、装卸和依赖不变。

# 座椅透视与二维接触点（2026-09-15）

按参考图及补充要求，座椅改为接近正面并进一步朝左约25度的原创无扶手座椅，补齐可见金属前后椅脚；货架/托盘采用浅俯视、可见窄右侧面的透视。运行资源为 `cabin-seat-v3.png`、`cabin-pallet-v3.png`，来源和提示词见 `art/cabin-layered-v3/prompts.md`。

本增量替换此前“人物与座椅同高”和独立前扶手图层的约定。6位乘客分别记录归一化臀部x/y与脚底y：人物臀部同时对齐坐垫承重点的两轴，脚底与椅脚落在同一地板基准；缩放前共同约束人物和座椅边界。货物底部对齐托盘顶面，文字仍在独立下方区域。只改美术投影，不修改经营、容量、订单、存档或结算。

# 分层机舱资源（2026-09-15）

机场装载页改用内置 image_gen 生成的原创左向分层位图，取代本文件旧的暂用SVG剖视边界。保留机库/商店既有左向v3外观和地图3D航迹方向。

| 资源 | 用途 |
|---|---|
| `cabin-hull-{light,regional,heavy}-v1.png` | 加厚机身三档，已头朝左 |
| `cabin-interior-{passengers,cargo}-v1.png` | 独立空客舱、空货舱背景 |
| `cabin-seat-left-v1.png` | 独立左向空座椅，按锚点摆放 |
| `cabin-pallet-v1.png` | 独立空托盘，按锚点摆放 |

原始图集和完整生成/修订提示词见 [prompts.md](../art/cabin-layered-v1/prompts.md)。`scripts/extract-cabin-art.ps1` 只按已校准矩形提取原图RGBA，不重绘、不重采样。源图透明像素可能保留背景RGB，浏览器按真实alpha合成。美术保持空舱，乘客货物仅来源于真实订单；无APK素材随产品分发。坐标、分层、容量和分页规则见 [AIRCRAFT-CABIN.md](AIRCRAFT-CABIN.md)。

> 2026-09-14 更新：本文件下方保留历史美术记录。当前规则和新增范围以 [OFFLINE-CAREER.md](OFFLINE-CAREER.md) 与 [APK-EVIDENCE.md](APK-EVIDENCE.md) 为准。

## 左向飞机位图 v3（2026-09-15）

新增 `aircraft-{light,regional,heavy}-{passenger,cargo}-v3.png`，6张均为1200×540 RGBA，机头统一朝左。内置imagegen先生成右向高分辨率草图，保存右向原图，再无损水平镜像、按透明轮廓裁切并输出统一画布；右向原图和左向高清源保存在 `art/aircraft-reference-v3/`，提示词见同目录 `prompts.md`。现代机型目录从v2切到v3，覆盖商店、主机场地面、机库和地图回退；v2文件保留供旧引用，不删除。这替换下文“机型分类示意仍保留既有实现”的限制，但剖视机舱结构仍是暂时通用SVG，后续美术资源必须补位图并继续头朝左。

## 参考货物品类扩充（2026-09-15）

用户确认扩充货物名称与美术，沿用现有价格、容量与运输规则。本轮使用原作 `StaticGoods.js` 的24种货物作为品类参考，以通用名称和原创图形呈现；不是原作62条货物配置或194种产业商品的完整复刻。三张1774×887 RGBA图集由内置 image_gen 生成，原图/提示词/源货物编号见 `art/cargo-reference-v1/`；从未将APK图集或裁片加入运行时。

| 原创图集 | 品类 |
| --- | --- |
| `cargo-food-atlas-v1.png` | 牛排、草莓、西瓜、雪花梨、苹果、葡萄、大米、鲜鱼 |
| `cargo-everyday-atlas-v1.png` | 旅行箱、针织衫、方便面、毛绒玩具、电脑、木箱、邮政包裹、旅行袋 |
| `cargo-special-atlas-v1.png` | 石油、煤炭、宝石、药品、救灾物资、酒类、羊驼、白色货箱 |

图集原样复制到 `public/art/`，运行时清单增至47项，全部走BASE_URL与PWA预缓存。每个物体按实际轮廓坐标加5像素余量进行SVG裁切，不按均分网格切割：鲜鱼、羊驼等跨过了数学中线。每实例clipPath防止相邻物体出现在留白区，不镜像新货物素材。

展示映射在 `src/ui/cargo-art.ts`，不写入经营状态。普通服务显示行李、衣物、食品水果、电脑、木箱、宝石、救灾物资、酒类、羊驼等；快件显示包裹、行李、玩具、电脑、衣物及救灾物资；冷链显示药品、鲜鱼、牛排、草莓及葡萄；工业显示石油、煤炭、电脑及木箱。订单ID决定该服务集合中的稳定外观，不按位置、时间或页面重新随机。上述只是游戏展示，不新增真实航空货运规则或不同货名的费率。

`product` 不为空的库存运输保持 `MATERIALS` 真名，使用对应的示意图，不伪装成新库存货种。候运普通货单显示货名及原金额，机舱维持目的地/金额两行，货名见悬停与可访问名称；中文/英文同步。原服务货位、改装要求、报酬、结算、存档格式全部保持，旧图继续保留兼容引用。

## 乘客站姿与坐姿替换（2026-09-15）

接入用户确认的微侧身版原创图集 `public/art/passenger-standing-seated-v2.png`（1774×887，RGBA），保留6位人物的12个姿态。候运区显示上排站姿，客舱显示下排坐姿；同一订单通过原有 ID 求和取模算法保持同一人物，装卸、刷新、离线和存档恢复不改变外观编号。图集原生接近正面、略偏左，乘客不再做水平镜像。新货物遵循上方品类增量，飞机和空座椅沿用原美术与镜像规则。

源图原样保存在 `art/passenger-reference-v2/`，提示词见同目录 `prompt.txt`，使用内置 image_gen 生成；仅参考原作人物角度与画法，没有将 APK 图集或裁片接入游戏。通过 SVG 的 viewBox 与每实例独立 clipPath 读取对应人物区域，周围保留4像素透明余量，不重新绘图或拆分 PNG。裁切同时限制宽屏留白区，避免显示相邻人物。坐姿不带椅子，叠加在已有机舱座椅后，人物层位于座椅装饰之上，避免脚部被覆盖。

共享图集通过 BASE_URL 和现有 PWA glob 预缓存，连同上方3张货物图集，当前清单共47项。原6张 v1 人物仍供既有服务图标等兼容引用使用，客货装载人物已切换至新版。核心规则、存档版本、数据库、依赖与场景布局不变。浏览器验收覆盖站坐转换、外观编号、朝向、桌面/手机横屏与75%–150%缩放、离线解码及刷新装卸；实际结果记录在 `art/VALIDATION-passenger-v2.md`。

## 机场剖视机舱（2026-09-15）

停靠装载场景改用代码绘制的原创通用 SVG 剖视机体，复用既有乘客/货箱 PNG，舱内座椅和空托盘为原创 SVG/CSS。真实机型名称和客货容量仍由当前飞机投影。此处替换下文停靠飞机 PNG 加容量图标的展示条款；飞行、机库、商店及地图美术不变。详见 [AIRCRAFT-CABIN.md](AIRCRAFT-CABIN.md)。

## 地图 3D 飞机（2026-09-15）

地图统一使用 `public/models/low-poly-airliner.glb`，由用户提供的 A 款文件原样复制，257,356 字节，无外部纹理。作者 Mauro3D，CC BY 4.0，模型 ID `f06d488f08764e3ca26f2917d4053c69`。完整署名、来源、许可、SHA-256 与运行时修改说明见 `public/models/ATTRIBUTION.txt`，中英文帮助页提供入口。运行时隐藏三组起落架，保留2组机体材质、2,284个三角面，统一长度52逻辑像素；不宣称各游戏机型的真实外形。GLB 和署名随 PWA 预缓存；原机型 PNG 继续用于机场、商店及地图加载失败回退。完整契约见 [MAP-AIRCRAFT-3D.md](MAP-AIRCRAFT-3D.md)。

## 点券图标与球面美术（v0.8.0）

新增 `public/art/icon-ticket-v2.png`（256×256，RGBA），运行时清单增至43项。该图使用内置 imagegen 原创生成，表现带侧边缺口、点线压纹与机翼徽记的金色票券，不含文字、货币符号、品牌或水印；用于顶部点券和帮助资源说明，原奖杯图标只继续用于经营中心。1254×1254透明原图保存为 `art/source/icon-ticket-v2-source.png`，完整提示词与缩放说明保存于 `art/prompt-ticket-v2.txt`。

地图没有新增或替换背景位图。球面在 PixiJS 中程序化增加海洋色阶、陆地光照、由离线陆地三角网格边界推导的海岸双描边、赤道层级和大气明暗边缘；航线增加二次抛物线高度，地图飞机复用机型配置的 `aircraft-*-v3.png` 本地透明图，不再绘制通用三角符号。继续使用原 Natural Earth 简化网格，所有绘制随相机按需更新。该升级不改变球面投影、机场坐标、航程和命中，也不引入在线资源。

## 单机经营扩展美术（v0.7.0）

新增14张原创透明PNG，运行时清单共42项。两张原始图均以纯绿背景生成，再抠除背景、修正绿边并输出RGBA；没有使用参考APK中的美术。提示词见 `art/prompt-career.txt`，源图位于 `art/source/aircraft-career-green.png` 和 `art/source/career-props-green.png`，处理脚本为 `scripts/prepare-career-art.py`。

| 成品 | 数量与规格 | 接入位置 |
| --- | --- | --- |
| `aircraft-{light,regional,heavy}-{passenger,cargo}-v2.png` | 6张，1000×450 | v2兼容引用；现代目录已切换到v3 |
| `cargo-{express,cold,industrial}-v2.png` | 3张，256×256 | 候运区真实快件、冷链与工业货物 |
| `facility-{warehouse,factory,design,research,trade}-v2.png` | 5张，384×320 | 物流园五座可点击建筑，带实际设施等级和升级操作 |

所有图片走本地BASE_URL和PWA预缓存。洲际与远程共享大型剪影，混合机沿用客机剪影；空中飞行继续采用已有通用机体。并未绘制原作全部载具、活动图或独立逐机型飞行动画。

# 美术资源 · 第一、二批

## 客货候运地面补充（第五批交付）

新增 `public/art/apron-platform-v1.jpg`（1536×1024，RGB），用于全宽客货区背景，累计28项运行时成品。使用内置imagegen原创生成空站台铺装、安全线、路缘灯与深色地面，不含人物、货箱或文字。原图 `art/source/apron-platform-v1.png`，提示词 `art/prompt-apron-platform.txt`；运行 `python scripts/prepare-platform.py` 编码JPEG并更新清单。该背景为不透明图片，不需绿底抠图；既有透明人物、飞机和图标仍沿用绿底生成后抠图的成品。背景通过BASE_URL读取并纳入PWA离线缓存。

后续页面调整见 [AIRPORT-REFERENCE.md](AIRPORT-REFERENCE.md)：人物和货箱直接置于地面，机体载荷叠层始终显示，已移除开关。成品仍沿用本文件27项资源；第三批交付是布局与按钮修复，未新增位图。

2026-09-12 使用内置 imagegen 生成，参照用户给出的明亮卡通航空经营视觉方向；不裁取参考图、不使用真实航空公司商标。素材是原创生成的游戏化示意，不代表真实机场或真实机型，也不表示整套界面已完成参考效果。

| 成品 | 规格 | 当前用途 |
| --- | --- | --- |
| `public/art/airport-day-v1.jpg` | 1536×1024，不透明 | 通用地面机场背景；机场名称仅在顶部状态区显示 |
| `public/art/passenger-01-v1.png` 至 `passenger-06-v1.png` | 各256×320，RGBA | 6位独立旅客，候机与机上订单卡共用 |
| `public/art/cargo-v1.png` | 256×256，RGBA | 普通货物订单 |
| `public/art/aircraft-v1.png` | 1408×640，RGBA | 主场景地面机体，第二批已接入，叠加真实载荷示意 |

旅客外观仅为装饰，与票价、职业或其他经营属性无关；旧聚合订单继续保留真实数量说明。未增加快件、冷链、特殊货物等尚无核心规则支持的货种，也未把参考图中的货币和功能按钮加入游戏。

`art/source/` 保存4张生成原图。角色图集、货箱、飞机按不透明 `#00FF00` 纯绿背景提示生成；模型输出的绿色有少量色值偏差，因此先将背景区统一为精确 `#00FF00`，保存为 `*-green-solid.png`，再执行抠图；主体边缘保留过渡像素供羽化处理。`art/prompts.json` 保存本批全部完整提示词。`*-keyed.png` 是去背景后的原分辨率中间文件，`art/manifest.json` 记录成品尺寸、文件大小与透明度检查结果。

复现处理：安装 Python 与 Pillow 12.2.0 后执行 `python scripts/prepare-art.py`。脚本按绿色相对红蓝的溢出量抠背景，对边缘羽化及去绿，裁出图集的3×2独立角色，再按统一画布尺寸输出；PNG保留透明通道。这个流程要求主体没有绿色配色，后续绿色服装或物品需另用遮罩，不能直接套用本脚本。生成模型不属于本地构建依赖，正常 `npm run build` 直接使用已保存的成品。

`art/review/sprites-light-dark.jpg` 展示同一透明层在浅色与深色底上的边缘。实际页面的飞机使用位图、装载状态使用只读SVG叠层；飞行中继续使用云层场景。Vite资源路径通过 `BASE_URL` 支持根路径与现有 `/China-Airlines/` 部署路径，所有成品纳入PWA预缓存。

本批只交付本地功能分支和ZIP；没有执行提交、PR合并或部署。第一批检查结果见 `art/VALIDATION.md`，第二批见 `art/VALIDATION-v2.md`。

## 第二批：按钮、地勤与飞机替换

新增18张透明PNG，累计27个成品。均使用内置imagegen；完整提示词在`art/prompts-v2.json`、绿底纠正提示在`art/prompts-v2-corrections.json`，飞行机体提示在`art/prompt-aircraft-flight.txt`。原始不合格黑底输出另存为`*-rejected-background.png`供追溯，不进入运行时；经imagegen改为绿底后再标准化、抠图。

| 成品 | 规格 | 页面用途 |
| --- | --- | --- |
| `icon-airport/map/directory/plane/shop/task-v1.png` | 各256×256 | 机场装载、航线地图、机场目录、机队管理、飞机商店、运营任务；飞机图标也用于起飞 |
| `icon-coin/trophy/save/help/maintenance/energy-v1.png` | 各256×256 | 资金、完成航班、存档、帮助、机库改装、补能 |
| `pilot-avatar-v1.png` | 320×320 | 左上品牌头像 |
| `tug/baggage-trailer/ground-crew/cones-v1.png` | 各384×320 | 地面场景装饰层，不表示订单装卸进度或真实地勤结算 |
| `aircraft-flight-v1.png` | 1408×640 | 起落架收起的飞行状态机体 |

图标居中留白，配色使用蓝、白、金，帮助问号和确认勾等语义清晰。按钮沿用中文名称、键盘焦点、禁用状态与点击区域。确认勾、旋转提示等简洁通用符号仍使用SVG；不为当前不存在的功能增加按钮。大型机、货机暂共用主场景机体示意，以真实机型名称、客货数量和容量叠层为准，不声称已完成逐机型外形。商店已使用v3位图；机场剖视机体是阶段边界内保留的暂时SVG。

处理脚本记录导航/工具3×2图集的切片顺序与地勤图集实际分界线；地勤员头盔跨过数学中线，按空白间隔`440/1024`切割，防止拖车或地勤员被错误裁切。成品纳入同一PWA预缓存，所有图片由新增离线解码回归逐张验证。无新增动效或在线依赖。
