name: Verify
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: verify-${{ github.ref }}
  cancel-in-progress: true
jobs:
  verify:
    name: verify (${{ matrix.name }}, shard ${{ matrix.shard }}/2)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: root
            base_path: ''
            shard: 1
          - name: root
            base_path: ''
            shard: 2
          - name: subpath
            base_path: /China-Airlines/
            shard: 1
          - name: subpath
            base_path: /China-Airlines/
            shard: 2
    env:
      BASE_PATH: ${{ matrix.base_path }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Install exact locked dependencies
        run: npm ci
      - name: Audit development and runtime dependencies
        run: |
          mkdir -p artifacts
          npm audit --audit-level=moderate --json > artifacts/npm-audit-all.json
          node --input-type=module -e "import fs from 'node:fs'; const report=JSON.parse(fs.readFileSync('artifacts/npm-audit-all.json','utf8')); if(!report.metadata?.vulnerabilities) throw new Error('Audit service did not return a vulnerability report'); console.log(report.metadata.vulnerabilities);"
          npm audit --omit=dev --audit-level=moderate --json > artifacts/npm-audit-runtime.json
      - name: Typecheck
        run: npm run typecheck
      - name: Domain and persistence tests
        run: npm test -- --reporter=default --reporter=json --outputFile=artifacts/unit-tests.json
      - name: Production build (${{ matrix.name }})
        run: npm run build
      - name: Install Chromium and system fonts
        run: |
          npx playwright install --with-deps chromium
          sudo apt-get install -y fonts-noto-cjk
          mkdir -p artifacts
      - name: Browser verification (${{ matrix.name }})
        # Each shard keeps the configured single worker and zero retries.
        # Both shards are required; no test filters or reduced assertions.
        run: npm run test:e2e -- --shard=${{ matrix.shard }}/2
      - name: Upload compact verification reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: china-airlines-reports-${{ matrix.name }}-${{ matrix.shard }}
          path: |
            artifacts/*.json
            test-results/**/error-context.md
            test-results/**/*.png
          if-no-files-found: warn
          retention-days: 14
      - name: Upload browser evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: china-airlines-verification-${{ matrix.name }}-${{ matrix.shard }}
          path: |
            artifacts/
            playwright-report/
            test-results/
          if-no-files-found: warn
          retention-days: 14
