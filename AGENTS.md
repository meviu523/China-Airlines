# 中华航空 · 开发约定

开始工作前必须阅读本文件、`docs/DESIGN.md` 和相关代码。若后续需求与设计冲突，先明确指出冲突，再调整设计与实现。

## 产品目标
- 目标为航空版《中华铁路》：保留场景式页面结构、客货选择装载、运输规划和载具经营的操作习惯，不以通用管理后台或凭空生成载荷代替核心体验。
- 未对照确认的数值与功能必须注明阶段边界，不宣称完整一比一复刻。

## 不变约束
- 横屏优先、中文、2D场景及球面投影航网、单机 Web/PWA。首版不依赖账号、业务后端、外部地图、在线字体或遥测。
- TypeScript + Vite + React + PixiJS 8 + Zustand + Dexie/IndexedDB。
- `src/core` 是经营状态的唯一写入者，不依赖 DOM、React、PixiJS 或数据库。UI 只发送命令；地图只读快照，不结算收益。
- 到达与离线收益按时间事件结算，不依赖动画回调；禁止重复结算。离线补算上限与时间回拨规则写入设计。
- 导入存档必须严格校验且失败不得覆盖有效存档；保留备份；数据库结构版本与游戏存档版本分别管理。
- 不复制参考游戏的商标、图片、音效或代码。坐标和机型数值是游戏化配置，地图不是导航或行政边界资料；全球机场与球面交互遵循 `docs/GLOBE.md`，只接受当前存档格式，版本不符必须拒绝。
- 修改规则时同时修改设计和测试。禁止上传密钥、用户存档、node_modules。

## 验收与交付
- 运行类型检查、核心单元测试、生产构建及 Playwright 浏览器流程；未运行或失败的检查必须明确记录，不得宣称通过。
- 验证桌面横屏、手机横屏、竖屏提示、刷新恢复、导入/导出、离线启动。
- 在功能分支开发，通过 PR squash 合并；不得覆盖其他工作或绕过分支保护。
- 日常修改合并后使用仓库已配置路径直接部署，不再重复生成源码 ZIP 或静态构建 ZIP；仅在用户明确要求离线交付包时按需打包。部署权限、账号套餐或外部服务缺失时如实报告，不改变仓库可见性，不购买服务。
